# 068：流道非线性收敛诊断与重复求解优化

- 状态：done
- 优先级：P2
- 依赖：058 / 062 / 063
- 来源：2026-09-15 整体复核；承接 063 以外的明确剩余项

## 背景与现状

`solveTree()` 使用 500 次上限与 1e-14*Q 的停止条件，到上限静默返回。
各 gateFlow/gateTemperature/pressureDrop 查询重复求整个网络；目前没有
证明这些重复计算在代表性多浇口模型中的成本占比。

## 目标与非目标

先使每次网络求解能证明收敛，再按测量结果缓存一致输入下的全部叶子结果。
不预先承诺加速比例，不改动 062 的逐浇口指定流量语义。

## 工作拆解与涉及文件

1. `moldingRunnerNetwork`：输出迭代数、流量守恒及路径压力残差；不收敛
   应明确告警/拒绝，阈值由有量纲绝对项与相对项组成。
2. 构造不对称树、强剪切变稀、阀关闭、低流量和不等温的模型级对照。
3. 在固定 Q/t/配置版本下缓存叶子流量/温度/压降，失效键包含全部影响
   网络的状态；不可只按 timeIndex 缓存，PIMPLE 中 Q 会变。
4. 与 062 的拷贝构造、Function1 生命周期及超配规则对齐；按同会话记录
   迭代次数和墙钟，验证缓存开关不改物理结果。

## 验收标准

收敛结果通过独立守恒/路径压降检查；强制低迭代上限能触发失败断言。
缓存开关在相同输入逐点一致，改变 Q/t/阀状态不返回旧结果。
本地只跑小网络模型与秒级基准，收益不足则保留阴性结论、不引入缓存。

共享 DoD 仍适用：构建无新增告警、相应最小测试及完整 CI/契约验收，记录
提交哈希与取证口径后再标 done。遵循 `../diagnostics.md`。

## 风险与缓解

默认容差过紧可能浪费迭代，但不能未量化就放宽。共享结果的所有权及
边界 clone() 必须明确，避免重演 062 的空项 PtrList 拷贝问题。

## 实施记录 2026-09-15

树网络达到最大固定点迭代次数而未满足流量容差时现在发出明确告警，
并说明返回的是有界末次迭代。缓存键和逐点收敛缓存仍需独立设计与基准，
因此任务保持进行中。

### 改动前后对比

| 指标 | 改动前 | 改动后 | 口径 |
|---|---|---|---|
| 收敛容差 | 固定 `1e-14` | 默认仍为 `1e-14`，可显式配置 | of14 runnerTree 测试结果不变 |
| 未收敛可见性 | 达上限后静默返回 | 输出告警并说明末次迭代 | 不改变末次值 |
| 运行性能 | 基线未单独测量 | 默认无额外迭代；告警仅在异常路径 | 2000 次 runnerTree 成本测试通过 |

已实现可选 `cacheRunnerSolve`：仅缓存最后一次相同 `Q/t` 的叶流量、温度和压降，
改变任一键即失效，默认关闭。缓存开关未改变默认结果；缓存收益/命中率尚未做
墙钟结论，避免把小模型噪声当成性能提升。

### 缓存键验收 2026-09-15

在 of14 的 `xmake run test` 中新增 opt-in 缓存断言：相同 `Q/t` 返回逐点相同
结果，改变 `Q` 会重新求解并改变压降，改变 `t` 也会触发键失效（本例物理结果
随时间不变，故数值保持一致）。模型测试全部通过。当前仍不宣称墙钟收益；多浇口
长时序命中率和开关对比留给 nightly/专项基准。

收敛容差现可通过 `treeConvergenceTolerance` 显式配置，默认值保持 `1e-14`；
默认精度不变，后续基准可在不改代码的情况下比较容差与成本。


### nightly 收口 2026-09-15

Nightly run 34946060723 的 model-tests 与 solver-cases 通过；缓存键失效和收敛告警覆盖完成。
