# 016 — 流道/热流道耦合

- 状态：done（2026-09-10：1D 流道网络模型 + 入口/保压边界耦合已落地
  并解析验证；热流道温度控制经可选的段壁温/换热系数支持）
- 优先级：P3
- 依赖：001/006
- 预估规模：1–2 周

## 1. 背景与现状

当前契约 case 把浇口当作注入口边界（给定流量/压力曲线），没有显式
流道系统。真实热流道/冷流道中：

- 流道内压力损失影响实际浇口压力与充填平衡；
- 热流道温度控制影响熔体入口温度；
- 多浇口分流需要流道网络平衡。

## 2. 目标与非目标

### 目标
1. 一维流道网络模型（压力-流量-温度沿程）与三维型腔耦合；
2. 支持热流道温度控制（等温/加热功率）；
3. 多浇口分流。

### 非目标
- 流道三维网格共轭求解（可与 008 结合）；
- 冷料井/废料处理。

## 3. 技术方案

- 路线 A（推荐起步）：**1D 流道网络**（等效阻力 + 能量方程），作为
  多浇口边界条件的上游，与型腔入口耦合（压力/流量/温度迭代）；
- 路线 B：流道也划入三维网格，与型腔同一 VoF 求解（简单但网格量大）；
- 与 006 的 V/P 切换衔接：流道网络参与保压压力传递。

## 4. 工作拆解

1. 网络拓扑/参数字典（直径、长度、温度）；
2. 1D 求解与三维入口耦合迭代；
3. 多浇口分流平衡验证；
4. 演示 case 与文档。

## 5. 验收标准（DoD）

- 单流道压力损失与解析 Hagen–Poiseuille 对拍；
- 多浇口流量分配与阻力比一致；
- 缺省（无流道）行为不变；
- CI 双架构绿。

验收记录：

- `Foam::moldingRunnerNetwork`：主流道（串联）+ 多浇口（并联）圆管
  网络；每段广义 Hagen–Poiseuille 压降 `dp = 128ηLQ/(πD⁴)`（η 由
  常数/幂律/CrossWlf 在壁面剪切率 `32Q/(πD³)` 求值），并联按等压降
  分流（定黏度 = 阻力比解析解；同幂律指数 `Qi/Qj=(Dj/Di)^(3+1/n)`），
  熔体温度沿段按一维稳态能量平衡演化（可选 `wallTemperature`/`htc`）；
- model tests：Hagen–Poiseuille 手算点（rtol 1e-12）、串联精确复现、
  定黏度分流比 16、幂律分流比 32（rtol 1e-6）、壁耦合温度指数
  （rtol 1e-12）；
- 求解器耦合：`moldingInletVelocity` 可选 `runner`/`gate`/
  `totalFlowRate`（入口流量取网络分流，支持多浇口）；
  `moldingPrghPressure` 可选 `runner`（保压期闸口压力扣除流道压降）；
- 集成用例 `tests/cases/runnerNetwork`：单浇口网络入口质量流与 ρQ
  一致（1.6%）；`xmake run test-solver` 6 用例全绿；
- 缺省不写 `runner` 时行为与旧版一致（原 `volumetricFlowRate` 路径）。

## 6. 风险与缓解

| 风险 | 缓解 |
|------|------|
| 网络-三维耦合收敛慢 | 松弛迭代 + 初值继承 |
| 材料在流道内的温度演化 | 1D 能量方程与 CrossWlf 一致 |

## 7. 涉及文件

| 文件 | 改动 |
|------|------|
| `src/moldingFoam/` 新流道模型 | 1D 网络 |
| 入口边界 | 与网络耦合 |
| `case-contract`/演示 case | 流道参数 |
